import { cn } from "@renderer/utils/cn";

export default function DesignPage() {
  return (
    <div
      className={cn(
        "flex flex-1",
        "p-6",
        // "flex flex-1 flex-col h-full",
        // "rounded-(--radius-card) bg-surface border border-border p-4",
      )}
    ></div>
  );
}

