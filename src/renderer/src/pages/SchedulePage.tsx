import { Card } from "@renderer/shared/ui";

export default function SchedulePage() {
  return (
    <div>
      {/* 헤더? - 일정의 날짜들을 컨트롤하는 패널 */}
      <Card className="shrink-0">
        {/* 
        1. 일/월별
        2. 일/월 표기
        3. 이전/오늘/다음 일/월 이동 버튼
        4. 일정 갯수
        5. 일정 추가 버튼
         */}
      </Card>
    </div>
  );
}
