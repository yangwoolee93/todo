/// 기기 간 병합에서 서로 겹치지 않는 전역 고유 id를 만든다.
/// (기존에는 생성 시각·순차 증가값을 id로 썼는데, 두 기기가 각자 만든
/// 항목이 같은 id를 가질 수 있어 병합 시 서로 다른 항목이 하나로
/// 뒤섞이는 문제가 있었다.)
pub fn new_id() -> String {
    uuid::Uuid::new_v4().to_string()
}
