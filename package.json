{
  "name": "orbit",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "desktop": "pnpm --filter @orbit/desktop tauri dev",
    "desktop:build": "pnpm --filter @orbit/desktop tauri build"
  },
  "devDependencies": {
    "nx": "^21.0.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
